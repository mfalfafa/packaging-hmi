const state = {
  webserviceUrl: 0
}

const mutations = {
  updateWebServiceUrl (state, val) {
    state.webserviceUrl = val
  }
}

// const actions = {

// }

export default {
  state,
  mutations
//   actions
}
